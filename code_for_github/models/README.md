# Model placeholders

The repository includes local model configuration/tokenization/modeling files needed by the current training code, but does not include large pretrained weights.

Place each backbone's local HuggingFace-style checkpoint files into the corresponding subdirectory, or update `MODEL_PATHS` in the training scripts.
